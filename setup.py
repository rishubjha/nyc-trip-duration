from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An ml project to predict the trip duration for nyc taxi servise.',
    author='Rishub Jha',
    license='MIT',
)
